# Verified against the environment used for the paper experiments
# (Python 3.13, CUDA 12.8). Newer minor versions generally work; the
# transformers 5.x / DeepSpeed 0.18.x pair is the one that was tested.
torch==2.10.0
transformers==5.6.0
deepspeed==0.18.9
accelerate==1.13.0
datasets==5.0.1
huggingface_hub==1.26.0

# Optional: only needed for --rollout_backend vllm_ray and
# --generation_backend vllm in generate_reference_responses.py
vllm==0.19.1
ray==2.55.1

# LLM-as-a-judge winrate evaluation
openai==2.32.0

# Logging, plotting, utilities
wandb==0.26.0
matplotlib==3.11.0
numpy==2.2.6
scipy==1.17.1
pandas==3.0.5
tqdm==4.70.0
psutil==7.2.2
nvidia-ml-py
